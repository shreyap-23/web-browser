/**
 * Multiplies two numbers.
 * @param {number} x - The first number.
 * @param {number} y - The second number.
 * @returns {number} The product of x and y.
 * @example
 * multiply(2, 3); // returns 6
 */
function multiply(x, y) {
    return x * y;
  }

  // Write a function to make it uppercase
  
  /**
   * converstion of lowercase to uppercase
   * @param {string} s - given string
   * @param {string} - to upper case 
   * @returns 
   */
const upper = (s) => s.toUpperCase();
let s = "hello!!";
console.log(upper(s));
 
/**
   * converstion of lowercase to uppercase
   * @param {string} s - given string
   * @param {string} - to upper case 
   * @returns 
   */
function coverToUpperCase(text){
    return text.toUpperCase();
}
text = "shreya";
console.log(coverToUpperCase(text));